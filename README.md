# Test Kubernetes Bundle Resources

E2E test repo for `kubernetes-bundle-resources` workflow in buildon-github-actions.

Tests compound-file-pattern-match versioning (major.minor from Dockerfile ENV, patch from PATCH_VERSION file), lower-kebab name style with ALL name validation, preserve-all trailing newline mode, and custom config-sub-path.
